# レビュー: SSE（Server-Sent Events）入門 — サーバープッシュをHTTPだけで実現する

**対象記事**: `articles/server-sent-events-basics.md`
**レビュー日**: 2026-06-21
**レビュー回数**: 1回目
**総合スコア**: 79点 / 100点

---

## 評価サマリ

| 観点                   | スコア | 判定    |
| ---------------------- | ------ | ------- |
| 内容の正確性           | 22/25  | ◎       |
| 文章の読みやすさ・構成 | 21/25  | ◎       |
| Markdownの活用         | 17/25  | ○       |
| 読者への価値提供       | 19/25  | ○       |

判定基準: ◎ 20-25点 / ○ 15-19点 / △ 10-14点 / ✗ 0-9点

総評: 技術的に正確で、文体も模範的に整った良記事です。ただし「はじめに」で約束した内容と本文に食い違いがあり、コード例ゼロのため実装目的の読者には物足りません。小さな修正で 85 点台に届きます。

---

## 1. 内容の正確性: 22/25

### Good

- 技術的な記述はほぼ全て正確です。「HTML Living Standard で定義された標準仕様」「UTF-8 テキストのみ」「ブラウザは1ドメインあたり約6接続まで」「HTTP/2 なら多重化で緩和」など、初心者が誤解しやすい点を正しく押さえています。
- 「ChatGPT や Claude の API のストリーミング応答も、実体は SSE 形式です」は時事性のある正確な補足で、読者の解像度を上げます。
- `X-Accel-Buffering: no`、`Last-Event-ID`、`fetch` と `ReadableStream` での代替実装など、実務の勘所が正確です。

### 問題箇所

> この記事で得られることは次の3つです。
> - SSE の仕組みと通信フロー
> - Node.js と EventSource を使った最小実装
> - WebSocket との使い分けの基準

2番目の「Node.js と EventSource を使った**最小実装**」が本文に存在しません。コード例を割愛したため、約束と中身が食い違っています。読者に「実装が学べる」と誤解させるため、**この行を本文の実態に合わせて書き換える**べきです（例: 「自動再接続や運用時のハマりどころ」）。

> 待ちは `retry:` で指定できます（既定は約3秒）。
> さらに `id:` を付けておくと取りこぼしを防げます。

`retry:` `id:` `data:` `event:` といったイベント書式のフィールドが、一度も定義されないまま登場します。事実は正確ですが、初出の用語が宙に浮いており、初心者は「この `retry:` はどこに書くの？」と迷います。技術的な誤りではないため減点は小さめですが、後述の「価値提供」に影響します。

---

## 2. 文章の読みやすさ・構成: 21/25

### Good

- です/ます調が全文で完全に統一されています。
- 一文がほぼ全て 60 文字以内に収まり、リズムが良く読みやすいです。お手本レベルです。
- 「はじめに」で対象読者（`:::message` 内）と得られることを明示できています。
- 「はじめに → 本題 → まとめ」の流れが自然で、見出しの粒度も適切です。

### 問題箇所

> - Node.js と EventSource を使った最小実装

（観点1と同じ箇所）構成の一貫性として、「得られること」で予告した項目に対応する本文セクションが欠落しています。予告と本文の対応が崩れると、読者は「読み飛ばしたかな」と不安になります。予告を実態に合わせるのが最優先です。

> 受け取るだけで済むなら、SSE がちょうど良い塩梅です。

「塩梅」はやや口語的で、他の硬めの文体とトーンが一段ずれます。気になるほどではありませんが、「ちょうど良い選択肢です」の方が記事全体と揃います（好みの範囲）。

---

## 3. Markdownの活用: 17/25

### Good

- インラインコードの使い分けが的確です（`text/event-stream`、`X-Accel-Buffering: no`、`Last-Event-ID` など）。
- WebSocket との比較を**表**で示しており、一覧性が高いです。
- `mermaid` のシーケンス図で通信フローを可視化できています。`:::message` も3回、補足として効果的に使えています。

### 問題箇所

> （記事全体）

**コード例が1つもありません。** 本スキルの評価基準は「コード例にコードブロック（言語指定付き）を使っているか」「『最小で動く』コード例が含まれているか」を明示的に重視します。概念中心の方針は理解できますが、ルーブリック上はここが最大の減点要因です。

- 折衷案: サーバー／クライアントの本格実装は割愛したままで構いませんが、「イベント書式」を示す **数行の `text` ブロック1つ**（`data:` と空行の例）だけでも置くと、観点2・3・4の課題（用語が宙に浮く問題）が同時に解消します。
- `:::details` は未使用です。補足的なコード断片を畳んで置く器として相性が良いです。

---

## 4. 読者への価値提供: 19/25

### Good

- **タイトルが秀逸です。** 「SSE（Server-Sent Events）入門 — サーバープッシュをHTTPだけで実現する」は、キーワード・得られること・引きの3点が揃っています。クリックしたくなります。
- 「WebSocket との違い」を軸に据えた切り口が良く、単なる公式ドキュメントの転記になっていません。
- 「ハマりどころ」（6接続制限・プロキシのバッファリング・ヘッダー不可・テキストのみ）は実務目線の価値が高いセクションです。

### 問題箇所

> - Node.js と EventSource を使った最小実装

繰り返しになりますが、約束した価値（実装）が提供されていません。読者の期待と提供のギャップは満足度を最も下げます。

> nginx などが応答をためると、データが届きません。

「ハマりどころ」は良い切り口ですが、**一人称の体験談がありません**。「私は nginx のバッファリングで丸一日詰まりました」のような一文があると、転記記事との差別化が一気に効きます。あなた自身の引っかかりを1つ入れると価値が跳ねます。

> （文字数 2,697字 / コード例なし）

入門記事としては適量ですが、コードが無いぶん「読んだあとに手を動かせない」読者が残ります。最小の書式例1つでこの不足感は埋まります。

---

## 改善アドバイス（優先度順）

| 優先度 | 課題 | 理由 |
| ------ | ---- | ---- |
| 高     | 「得られること」の「最小実装」記述を本文の実態に合わせて書き換える | 約束と中身の食い違いは誤解を招き、満足度を最も下げる |
| 中     | イベント書式の最小例（数行の `text` ブロック1つ）を1セクション追加 | `data:`/`id:`/`retry:` が未定義で宙に浮く問題を一挙に解消。観点2・3・4が同時に改善 |
| 中     | 「ハマりどころ」に一人称の体験談を1つ入れる | 公式ドキュメント転記との差別化。記事の価値が跳ねる |
| 低     | 「塩梅」を「選択肢」等に置換しトーンを統一 | 全体の硬めの文体と揃える（好みの範囲） |
| 低     | WebSocket 記事へのリンクは公開後にハードリンク化 | 現状はタイトル参照のみ。導線を確実にする |

## 次のアクション

修正後 `/fix-article` で自動修正、または再度 `/review-article` で再評価できます。
「高」の1点だけでも直せば 82 点前後、「中」2点まで対応すれば 86 点前後が見込めます。
